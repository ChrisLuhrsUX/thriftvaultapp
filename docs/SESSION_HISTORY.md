### Compressed Sessions, older than 3 days

- **v1.0 submitted to App Review (5/30)**. Production EAS build `96ea8a83` (buildNumber **7**); first attempt failed on missing Sentry org/project in EAS env. `eas submit` 401'd twice on manually uploaded ASC key `5R4H56CK65` (EAS stored Team/Roles None); fix = delete key on expo.dev + EAS-generated key `T64TPFN72R`. Build attached on Distribution → 1.0.0; blockers = primary category (Shopping) + Content Rights in App Information. TestFlight skipped; upload landing in TestFlight tab is normal processing UI. `docs/LAUNCH_BLOCKERS.md` fully checked. Pre-submit fixes: privacy manifest gained `NSPrivacyAccessedAPICategoryFileTimestamp` + `SystemBootTime` reasons; `android.permission.RECORD_AUDIO` removed + `recordAudioAndroid: false` in expo-camera plugin; "AI estimate" caption added above scan result price (4.3.1 disclosure); `userInterfaceStyle` flipped to `"light"`; `<ExpoStatusBar style="dark" />` added; camera permission string genericized (no "Google's AI"); PaywallModal gained explicit "Not now" button. ASC release option set to Manual. Pattern: multi-agent parallel audits systematically OVERSTATE 🔴 blockers; verify every Read/Grep before passing to user (3 agent-flagged blockers were false alarms or theoretical edge cases).

- **First EAS dev client build successful (5/27)** (`eas.json`, `app.json`). `eas device:create` for iPhone 13 UDID, generated Apple distribution cert + provisioning profile, built via `eas build --profile development --platform ios`. First attempt failed on Sentry source-map upload (`An organization ID or slug is required`); fixed by adding `SENTRY_DISABLE_AUTO_UPLOAD=true` to `eas.json:development.env`. Schema fix in same chain: moved `ios.minimumOsVersion` from `app.json:ios` to `expo-build-properties.ios.deploymentTarget`. iOS Developer Mode required for sideloaded apps on iOS 16+. Pattern: Expo schema fields rotate over SDK versions; when a field starts triggering `expo doctor`, search Expo changelog for migration target.
- **Sandbox purchase test, end-to-end (5/27)** (`.env`, RC dashboard + ASC config, `hooks/usePurchases.ts`). Swapped `.env` from `test_` to `appl_` RC key, hit chain of config gaps: RC App Store storefront had zero products (only Test Store from 5/26 wizard), fixed by uploading ASC API Key (`AuthKey_*.p8`) + wiring App-Specific Shared Secret + S2S Notification URL into ASC. Pro entitlement turned out to be `ThriftVault Pro` (with space + caps), not `pro`; swapped to `PRO_ENTITLEMENT_ID = 'ThriftVault Pro'` constant. Each `default` offering package had only Test Store product wired; added App Store products. Real StoreKit sandbox purchase worked end-to-end. Apple sandbox compresses 30-day trials to ~3 min. Pattern: RC's wizard creates Test Store products NOT auto-mirrored to App Store storefront, products + entitlements + offering-package wiring redone twice. RC entitlement identifier is locked at creation; document the exact string in a code constant.
- **`usePurchases` module-scoped state + AsyncStorage snapshot cache + Profile Pro card UI (5/27)** (`hooks/usePurchases.ts`, `app/(tabs)/profile.tsx`, `components/PaywallModal.tsx`). Refactored each `usePurchases()` callsite's per-instance `useState` to module-scoped `_entitlement` + `_subscribers` set. Full entitlement JSON snapshot cached to `tv_is_pro`; `_deserializeEntitlement` rejects non-object values so old boolean cache values fall through. Profile Pro card renders trial/renews/cancels state via `formatProSubtitle()`. Card tap deep-links to apps.apple.com/account/subscriptions. Pattern: when multiple components share external state via a hook, module-scoped state + subscriber set + force-update is the right shape; cache the FULL snapshot, not just a boolean.
- **LAUNCH_BLOCKERS restructure (5/27)** (`LAUNCH_BLOCKERS.md`). Marked First EAS dev client build + sandbox purchase test done with incident detail inline. New Pre-production build section with 3 items: `cli.appVersionSource`, prod env vars + RC `test_` to `appl_` swap, Sentry source-map vars. Share-as-image card moved from Post-prebuild to POST_LAUNCH.md.
- **App Store screenshot copy + Apple-Review-safe hedging (5/27)** (`STORE_LISTING.md`). 5 screenshot copy drafts; #4 hedged from "Spot fakes *before* you pay" (false-advertising risk) to "Catch the *red flags.*" (feature-accurate). Native iPhone 13 1170×2532 upscale ~1.13× in Figma to 1320×2868 (6.9" iPhone target). Pattern: screenshot headlines that promise certainty are App Review rejection bait; hedge to feature-accurate language that mirrors what the app actually does.
- **Sentry GitHub App permission upgrade denied (5/27)**. Sentry requested read + write Contents (for Seer AI bug-fix agent PRs); denied. Same threat model as 5/25 main-branch ruleset: AI-agent write access to repo contents is the attack surface the ruleset guards against. Retains read-only.
- **TikTok marketing prompt drafted, saved to `tiktok.md` (5/27)**. Prompt for Claude for Chrome to research 10 viral photo-carousel TikTok ideas for ThriftVault. First response returned format-level reasoning only (no actual browser nav) despite explicit instructions; drafted follow-up with step-by-step nav + stop conditions if browsing isn't enabled. Pattern: Claude for Chrome returns strategy-grade output without actually browsing if prompt allows wiggle room; explicit "open URL X first, return view counts, stop if blocked" forces real navigation.
- **Furniture form disambiguation HARD RULES added (5/27)** (`services/gemini.ts` FURNITURE PRICING block, lines 518–665, +~250 prompt lines). Three new blocks: TABLE TYPE (7 forms, Saarinen Tulip variant split into Side $400–$1200 / Coffee 36" round $800–$1800 / Dining $1500–$3500), CHAIR TYPE (3 binary tells, Eames Lounge replica $200–$600 carve-out, Aeron Classic vs Remastered split), DESK TYPE (6 binary tells, 10 forms). MCM authenticated tier extended with Papa Bear / PK22 / Womb / Egg / Swan / Series 7 / Aluminum Group / Pollock Executive / Bertoia / Emeco 1006 + new Modern ergonomic office subtier (Aeron/Embody/Cosm/Leap V2/Gesture). Pattern: free-form `name` field carries subtype signal across all three furniture categories; no enum changes needed. Plan-agent critique caught shipped-quality errors (dining seat height 17–19" not 18–20").
- **Drop-leaf coffee-table misclassification fix (5/27)** (`services/gemini.ts:545–550`). User scanned refurbished Ethan Allen oval drop-leaf coffee table; scan returned dining table at $280. Added leg-proportion heuristic (legs <50% of total height = coffee, 50–65% = side/nightstand, >70% = dining), drop-leaf/gateleg/Pembroke/butterfly-leaf rule (ignore leaves-up surface area), brand-association guard (Ethan Allen / Bassett / Lane / Drexel produce coffee+side+dining in same aesthetic; brand stamp alone NOT a routing signal). Pattern: positive-evidence guards fix brand-association bias cleaner than negative rules.
- **Chair red-flag extensions (5/27)** (`services/gemini.ts:877, 880`). MCM KNOCKOFF flag silhouette list extended with Aeron + 9 named chairs, per-form authentication surfaces spelled out (HM/Knoll/Fritz Hansen foil sticker, Eames Lounge 5-prong base, Aeron HM logo + yoke stamp + PostureFit SL + 8Z Pellicle pattern). STRUCTURAL DAMAGE flag extended with chair failure modes (recliner mechanism, gas-lift cylinder, Pellicle mesh, snapped spindle, missing castor wheel). All within EXHAUSTIVE LIST guardrail (5 furniture red-flag categories). Pattern: extending existing flag categories beats adding new ones when banner copy is already abstract enough.
- **Daily scan cap shipped (5/27)** (`utils/scanCap.ts` NEW, `services/gemini.ts`, `app/(tabs)/scan.tsx`, `app/detail.tsx`). Local-only abuse mitigation: `tv_scan_count_${YYYY-MM-DD}` AsyncStorage key (ISO via `toLocaleDateString('en-CA')`), `DAILY_SCAN_CAP = 100`, `ScanCapError` class. Gate at top of `scanWithGemini` / `refreshUpcycleIdeas` / `rescanAsHandmade`; increment AFTER successful return only (failures don't burn quota). UI: extended `ScanErrorKind` with `'cap-reached'`, hourglass icon variant. Patched 6 secondary catch blocks. Worst-case bound: ~$0.09/day Gemini on cap. Pattern: abuse protection (API cost cap) is operationally distinct from feature gating (paywall); `feedback_no_item_caps` does not apply.

- **3 ASC subscription products + RC↔ASC wired (5/26)**. Subscription Group `ThriftVault Pro` (id 22114945): `monthly` ($4.99/1mo w/ 30-day Intro Offer) / `three_month` ($9.99/3mo, no trial, Apple 1-intro-per-group rule) / `annual` ($29.99/1yr Upfront). Apple permanently burns deleted Product IDs; `yearly` got burned mid-creation, recreated as `annual`, asymmetric with locked RC package identifier `yearly` (memory `project_rc_asc_product_id_mapping`). RC wired via In-App Purchase Subscription Key + ASC API Key + App-Specific Shared Secret + S2S Notification URL (pending end-of-session). Pattern: ASC Subscription Group needs its own localization separate from per-sub localization; "Missing Metadata" badge persists until first binary upload regardless of metadata completeness.
- **react-native-purchases installed + config plugin error fix (5/26)** (`app.json`, `hooks/usePurchases.ts`). Modern RC SDK auto-links via Expo's native module discovery; no `app.json` plugins entry needed. Stale header-comment instruction added the entry and triggered `PluginError`; removed. Pattern: native-package setup comments rot fast; treat `app.json` plugins array as opt-in not opt-out.
- **constants/monetization.ts IDs renamed to match RC locked identifiers (5/26)**. RC wizard pre-fills `monthly` / `three_month` / `yearly` and BLOCKS rename. Changed `id: 'season'` to `'three_month'`, `id: 'annual'` to `'yearly'`; display labels unchanged (only `plan.id` is the runtime lookup key). Pattern: when a third-party tool locks identifiers in onboarding, rename YOUR code to match.
- **`.env` gained `EXPO_PUBLIC_REVENUECAT_API_KEY=test_...` (5/26)**. Memory `project_rc_test_key_swap`: must swap to `appl_` before production build. Sandbox tester `chrisluhrsdesign+sandbox@gmail.com` (Gmail `+sandbox` alias trick, base address burned as Chris's own Apple ID).
- **LAUNCH_BLOCKERS intra-day restructure: Async section added then commission cut (5/26)**. Morning: added "Async / parallel" section with human-illustrator icon commission anchored on AI-icon brand-contradiction risk. Evening: AI-icon prompt attempts didn't pan out, Apple confirmed no AI-icon ban, budget cut killed the commission. v2 PNG icon ships as-is. Pattern: budget-driven scope cuts often happen mid-day once real cost surfaces; keep planning docs fluid.
- **POST_LAUNCH cost-per-scan math + Gemini upgrade path (5/26)** (`POST_LAUNCH.md`). Daily scan cap (~100/day ceiling, $0.09/day worst-case Gemini) added between Affiliate links and Platform filter. Gemini 2.5 Flash deprecation: migrate to **3.1 Flash-Lite** ($0.25/$1.50 per 1M, ~3× cost, 45% faster gen, 2.5× faster TTFT), NOT 3.5 Flash (~15× cost would tank margin at ~10K-token prompt). 5-photo scan ~$0.0009 on 2.5 Flash, ~$0.035 on Claude fallback. Per-user monthly: $0.03 casual / $0.08 typical / $0.27 heavy / $0.68 power. Photo count NOT the cost driver (system prompt dominates). Pattern: model-migration plans must encode the COST cliff, not just speed/quality.
- **JPG to PNG asset sweep, 5 refs (5/26)** (`components/WebSidebar.tsx:84`, `index.html:8/9/857`, `app/(tabs)/scan.tsx:227`). `thriftvault_logo.png` is the single canonical asset; `.jpg` variants deletable (zero remaining refs).
- **Business email mailbox-recreate recovery (5/26)** (`contact@thriftvaultapp.com`, Namecheap Private Email). Locked out after losing 2FA authenticator (no backup codes). Trial-tier mailbox 5 days old with zero archived mail: `Remove` + `Buy Mailbox` to recreate in 2 min. Forwarding to `thriftvaultapp@gmail.com` via Gmail Accounts and Import POP3 pull (`mail.privateemail.com:995 SSL`). RC account registered with `contact@thriftvaultapp.com`. Pattern: trial-tier mailbox-recreate beats Namecheap 2FA-reset ticket.
- **Accessibility pass for App Review readiness (5/26)** (11 files + new `hooks/useReducedMotion.ts`). Toast announces via `AccessibilityInfo.announceForAccessibility` from `ToastContext.showToast` (Toast view is `pointerEvents="none"`); `accessibilityLiveRegion` is Android-only, every iOS announcement needs paired `announceForAccessibility`; `AppIcon` defaults to `accessibilityElementsHidden` when no label; new `useReducedMotion` hook + spring snap-instant for Paywall/BottomSheet; `CustomTabBar` `accessibilityRole="tab"` + `accessibilityState.selected`, container `"tablist"`; modal focus trap via `accessibilityViewIsModal`; composite `accessibilityLabel` on Profile stat rows / store rows / Vault Item+Haul cards; 8 TextInputs labeled; `accessibilityRole="header"` on every screen title + section header; PaywallModal closeBtn 44pt min + `hitSlop`. Pattern: audit subagents skew toward false positives; verify line numbers and computed contrast before applying recommendations.
- **POST_LAUNCH cleanup (5/26)** (`POST_LAUNCH.md`). Removed shipped: Landing page (deployed at thriftvaultapp.com) + Switch to `expo-image` (all 4 image surfaces migrated). Added: Dynamic Type support (HIG accessibility gap remaining for v1, polish not blocking). File-split items verified still valid TODOs: `detail.tsx` 2944 lines, `scan.tsx` 3463 lines, `services/gemini.ts` 1762 lines.

- **SESSION_HISTORY.md extracted from CLAUDE.md (5/25)**. Created sibling file, pulled out 5/21 session + entire "Compressed Sessions" block (~130 lines), compressed verbose 5/21 into 9 single-line entries. CLAUDE.md char count dropped from ~80k to ~34k. Updated `feedback_claude_md_compression.md` memory. Pattern: recent 3 dated sessions stay full-detail in CLAUDE.md; older lives in `SESSION_HISTORY.md`.
- **Main branch ruleset protection via GitHub API (5/25)** (ruleset id `16837336`). Branch ruleset on `~DEFAULT_BRANCH` blocks `non_fast_forward` + `deletion`, `bypass_actors: []`, `current_user_can_bypass: "never"`. Server-side defense against rogue AI tools acting under your credentials (4/29 Cursor/Opus prod-wipe incident scenario). Override path: Settings → Rules → "main protection" → Disable. Pattern lesson: ruleset `PUT` replaces full payload, not patches; to update one field, resend entire definition.
- **`gh` CLI installed via portable ZIP workaround (5/25)**. winget consistently failed with "Failed when opening source(s)". Fallback: `Invoke-WebRequest` `gh_2.92.0_windows_amd64.zip` (14.4 MB), `Expand-Archive` to `C:\Users\Chris\AppData\Local\Programs\gh\`, appended `bin\` to user PATH. Auth as `ChrisLuhrsUX` via device flow. Memory: `reference_gh_cli.md`. Pattern lesson: Git Bash on Windows rewrites leading slashes in `gh api` paths (`/repos/...` → `C:/Program Files/Git/repos/...`); omit leading slash. JSON payloads via `--input file.json`; `-f arrayfield='[]'` sends string not empty array.
- **Drift-watch PR #2 squash-merged (5/25)** (`pricing-drift-reports/2026-05-25.md` via `e64ef82`). 7 of 14 tiers flagged; eBay direct fetch 403'd during agent run so report leans Depop/Poshmark/resale-guide signal only (eBay 2× weight not applied). PR #1 (5/18) still open with critical Tiffany Studios authenticated lamp band `$500-$5,000+` off by 8-20× (real floor `$4,000-$10,000`); Coach canvas band needs split into pre-2000 leather ($80-$160) vs 2003+ C-canvas ($25-$45). Pattern lesson: when eBay direct 403s, verdict skews toward Depop's lower comp cluster; don't auto-apply recommendations from a 403-impaired run without manual eBay sold-listing re-check.
- **Landing hero reword (5/25)** (`index.html`, commit `c5c107a`, pushed `4796328`). "a worth-it or skip-it nudge" → "a heads-up on scams and counterfeits". Original middle-beat implied a binary buy/skip verdict feature that doesn't exist; new copy maps to the 3-way red flag classifier shipped 5/21 and parallels PaywallModal "Counterfeit & scam alerts" copy. Pattern lesson: every hero claim should map to a shipped feature; if prose contradicts product, find a real feature or drop the beat.
- **`LAUNCH_BLOCKERS.md` restructure (5/25)**. AI-logo swap moved from top of Pre-prebuild (#1) to first item in Submit section (doesn't gate critical-path code work). Pre-prebuild block fully checked. Screenshots step #7 expanded to Figma compose recipe: 6.9" iPhone 1320×2868 px required (ASC auto-scales to fill smaller sizes), 3-5 minimum, suggested order (scan-result hero / vault grid / paywall / red-flag banner / profile stats), Windows capture path (iPhone 13 native 1170×2532 upscale ~1.13× in Figma is reviewer-acceptable), brand `cream`/`vintageBlue` bg + Figma Community iPhone 16 Pro mockup + Playfair + DM Sans. Pattern lesson: Apple Screenshots consolidated on 6.9" (1320×2868) as of 2026; ASC auto-scales to fill 6.5"/6.7", one set suffices.

- **Kaftan/abaya HANDMADE_LARGE_DRESS clamp tier (5/24)** (`services/gemini.ts`, committed `4ddcec1`). `HANDMADE_LARGE_DRESS_RX` (kaftans/caftans/abayas/butterfly abaya/batwing/dolman maxi-or-gown/floor-length) + `isHandmadeLargeDress` clamp inserted before `isAlteredDress` and mutex'd. Floor $55, tier-aware cap $140 default / $160 premium fabric / $200 complex construction. Prompt-side: HANDMADE DRESS EXCEPTION re-tiered by SILHOUETTE + FABRIC YARDAGE in main PROMPT and HANDMADE_SUFFIX; WORKED EXAMPLE anchors stretch-satin kaftan to MODERATE ~$65-$85. Pattern: tier by silhouette + fabric yardage, not just construction complexity. Kaftan (~4 yds) and slip (~1.5 yds) are NOT the same labor floor even when both read as "plain knit".
- **Altered dress band tightening, supersedes 5/20 (5/24)** (`services/gemini.ts`). Simple $30-$50 to $25-$40. Moderate $50-$80 to $40-$65. Complex $80-$140 unchanged. Code clamp basic-fabric cap $80 to $65. WORKED EXAMPLE anchors updated. Trigger: user comp-flagged mint chiffon altered prom dress scan at $60/$50-$75, sold cluster actually $40-$60. Pattern: hobbyist altered prom/party reworks anchor lower than the model's first-pass estimate; comp-check periodically.
- **AI-fail inline error card UX (5/24)** (`app/(tabs)/scan.tsx`, ~50 lines). Replaced toast-only failure on `handleScanStaged` with persistent inline card in scan-result slot. New `ScanErrorKind` (busy/network/unavailable/parse/unknown), `classifyScanError`, `getScanErrorCopy` (all generic, no AI provider names). Try again + Clear via shared `<Button>`. `Sentry.captureException` with scope tag for production telemetry. Pattern: persistent inline state beats auto-dismissing toast on primary paths; a 2.6s toast leaves users with no path forward.
- **Library permission Alert + camera toast cleanup (5/24)** (`app/(tabs)/scan.tsx`). `handlePickFromLibrary` had no permission check; iOS silently returned `{canceled: true}` on denial. Added explicit `getMediaLibraryPermissionsAsync` + `requestMediaLibraryPermissionsAsync` pre-check; on denial fires native `Alert.alert` with Open Settings to `Linking.openSettings()`. Dropped redundant camera "access needed" toast since inline UI already shows Open Settings. Pattern: ImagePicker silently returns `canceled: true` on denied permission; pre-check + native Alert is canonical.
- **Microcopy sweep, 8 toasts normalized (5/24)** (`app/detail.tsx`, `app/(tabs)/scan.tsx`, `components/PaywallModal.tsx`). "Could not"/"Unable to" to "Couldn't" (4x); permission errors unified to "Camera access needed"/"Photo library access needed" (3 patterns to 1); "Purchases restored!" to "Purchases restored". PaywallModal "Welcome to ThriftVault Pro!" kept as celebratory moment. 56 toast strings audited across 7 files.
- **Haptics audit + shared `<Button>` default haptic (5/24)** (`components/Button.tsx`, `app/(tabs)/profile.tsx`, `components/PaywallModal.tsx`, `app/(tabs)/index.tsx`, 13 edits). Profile (8 Pressables, 0 haptics) + PaywallModal (14 Pressables, 0 haptics) had zero feedback. `Haptics.selectionAsync()` inside shared `<Button>` auto-fires ~10 callsites. Vault item/haul card taps intentionally NOT wired (navigation-only, iOS Photos/Calendar convention). Pattern: default haptic on shared `<Button>` covers most surfaces; navigation-only cell taps skip haptics per iOS convention.
- **Legal docs cleanup, 3 commits (5/24)** (`privacy-policy/index.html`, `terms/index.html`). (1) `30da1e9` disclosed Anthropic/Claude fallback in both docs + Terms gained Section 12 "Apple App Store Terms" with App Store Review Guidelines 3.2 EULA boilerplate; Last-updated April to May 2026. (2) `c16b029` dropped "AI scan companion" marketing phrase from both intros. (3) `a585b4b` dropped "a Tennessee limited liability company" descriptors from intros, kept Governing Law clauses. Memory saved: `feedback_no_tn_llc_in_legal_docs`.
- **Landing page polish, committed `a8509b9` (5/24)** (`index.html`). Hero lead "all on your phone" to "ready to list on Depop, Poshmark, eBay, or Vinted" (marketplace positioning, frames positive). Dropped "frankenpieces" from Handmade & upcycle pricing card.
- **POST_LAUNCH + DEV_OPS additions, committed `792538b` (5/24)** (`POST_LAUNCH.md`, `DEV_OPS.md`, `LAUNCH_BLOCKERS.md`). POST_LAUNCH gains 3 tech-debt items (useScanPipeline hook split, detail.tsx sub-component extraction, gemini.ts file split) + "Cross-list export to marketplaces" idea (clipboard v1, partnership path at ~10k+ active resellers). DEV_OPS gains "Mental model for the dev-client switch" subsection. LAUNCH_BLOCKERS step 5 gains blockquote callout pointing to it.
- **Memory saved: feedback_no_ai_provider_names (5/24)**. Never expose AI provider/model names (Gemini, Claude, etc.) in user-facing copy; use generic "AI" or omit. Internal classification for telemetry/logging is fine.
- **Smaller polish (5/24)**: `resultHeader` alignItems `'center'` to `'flex-start'` (price tag aligns with first line, matches Depop/Etsy). CustomTabBar scan-button press feedback opacity 0.9 to `transform: [{ scale: 0.96 }]`. Vault empty-search-state copy normalized ("No matches" title + parallel body across Flips/Closet and Hauls). Profile "About AI estimates" Alert copy expanded to include Chairish + Etsy for furniture/homewares.

- **Pre-launch responsive design pass for iPhone range + Dynamic Type (5/23)** (PaywallModal, onboarding, CustomTabBar, vault index, detail). PaywallModal sticky-CTA via ScrollView wrap + `maxHeight: '90%'` sheet + PanResponder rewritten to BottomSheetModal pattern; `features` dropped `maxHeight: 160` (SE 2/3 5th-feature clip after wrap). Onboarding: per-slide vertical ScrollView with `flexGrow: 1` + `justifyContent: 'center'` keeps default centered + scrolls under large Dynamic Type. `maxFontSizeMultiplier={1.3}` per-callsite on tab labels, vault search inputs, filter chips, detail pills. **Pattern lesson 1: Modal with draggable handle + scrollable content needs `onStartShouldSetPanResponder: () => false` + gated onMove (`g.dy > 4 && |dx|<|dy|`) or inner ScrollView never sees touch starts; BottomSheetModal at `components/BottomSheetModal.tsx:85-88` is canonical. Pattern lesson 2: nested ScrollViews of opposite axis work fine on iOS but inner vertical needs explicit `style.width = screenWidth` (not just contentContainerStyle). Pattern lesson 3: `maxFontSizeMultiplier` belongs per-callsite on fixed-height chips/pills at 1.3, NEVER globally via `Text.defaultProps` (caps body + AI rationale + legal text, regresses low-vision XXL users).**
- **SECURITY_AUDIT.md refresh (5/23)**. Finding 9 (`EXPO_PUBLIC_REVENUECAT_API_KEY` dormant in `usePurchases.ts:19`, RC iOS keys project-scoped, low exposure) + Finding 10 (`ascAppId: 6772308542` + `appleTeamId: UG3X275FNX` intentionally in `eas.json`, public-by-design once shipped). Methodology dropped stale TestFlight reference. Historical secret scan (`AIza` / `sk-ant` / `appl_`) clean.
- **Furniture upcycle vocabulary expansion (5/23)** (`services/gemini.ts:780,1556`). Furniture branch grew from one paragraph (~25 techniques) to 8 named categories (REFINISH / REUPHOLSTER / PAINT FRAME / HARDWARE SWAP / SURFACE-STRUCTURAL / RECANE-RERUSH / REPURPOSE / LIGHTING) + ERA-SHIFT RECIPES (80s oak → MCM, traditional → Japandi, particleboard IKEA → upgraded, brown → Scandi) + Q2 2026 spike list (color-drench, fluted/reeded, organic-modern, Japandi, Memphis revival) + FURNITURE BANNED defaults (farmhouse/shabby chic/country-rustic/boho beach/primitive). `buildUpcyclePrompt` regen path branches on `category === 'furniture'` with dedicated furniture-flipper persona. **Pattern lesson: when one branch of a category-conditional prompt is shallow vs sibling branches, the model fills the gap with the closest-fit branch's vocabulary (fashion-persona ideas leaking into furniture scans). Match branch depth to sibling depth.**
- **Before/After orientation bug, cover slot is authoritative (5/23)** (`services/gemini.ts:1031,1079-1083,1087-1096`). Mint-chiffon scan kept describing BEFORE source garment instead of AFTER. Three iterations: (i) AFTER ORIENTATION + DESCRIPTION ISOLATION HARD RULEs anchored on "more hand-construction tells" — model leaked; (ii) rewrote to "cover slot is AUTHORITATIVE for AFTER" — partial leak; (iii) three-layer defense: hoist rule to TOP of multi-photo branch + mid-suffix rules + tail FINAL DESCRIPTION CHECK with 4 binary YES/NO cross-checks (STRAPS / WAISTBAND / HEM / BODICE) + positive worked example + the model's exact wrong output labeled as negative case. Updated `feedback_photo_position_agnostic.md` memory. **Pattern lesson 1: rules asking the model to make inverted judgments ("polished isn't always newer") get inverted-wrong more than half the time; anchor on deterministic signals (cover slot identity) instead. Pattern lesson 2: HARD RULEs buried mid-prompt at line ~1080 of a 1500-line prompt get ignored even with forcing language; hoist to entry of relevant branch + add pre-emit FINAL CHECK at tail.**
- **LAUNCH_BLOCKERS banking-live + AI-logo-swap added (5/23)** (`LAUNCH_BLOCKERS.md:9,14`). Banking active on ASC; all 3 pre-prebuild gating items checked. New blocker line 14: swap AI-illustrated v2 app icon for human-made mark before submit. **Reverted 5/26**: budget cut killed the human-illustrator commission; v2 PNG icon ships as-is.

- **App Store Connect + Apple Developer infrastructure wired (5/22)**, Paid Apps accepted, W-9 active, banking processing. Apple Small Business Program enrolled (15% commission vs 30%). Bundle ID `com.thriftvault.app` registered with In-App Purchase + Data Protection. ASC app `ThriftVault: Scan to Flip`, Company `ThriftVault LLC`, SKU `thriftvault-ios-1`, `ascAppId: 6772308542`, `appleTeamId: UG3X275FNX`. `eas.json:20-22` filled. Icon re-exported: `assets/logo/thriftvault_logo.png` 1024×1024 8-bit colormap (no alpha), spec-compliant; all 4 `app.json` paths repointed. Pattern lesson: ASC New App form pre-validates client-side; when Company Name isn't yet surfaced for org accounts, the form throws a generic "validation errors below" banner with no inline indicator until the field renders. Symptom: `/iris/v1/apps` returns 409 Conflict (not a name/bundle/SKU collision, just a missing-required-field shape mismatch). Fix: wait/refresh for Company Name field to render, then fill.
- **`LAUNCH_BLOCKERS.md` created (5/22)**, Ordered punch list, source of truth for "what's left before submit". Sections: Pre-prebuild (parallel-safe) → After Paid Apps+Tax+Banking → Irreversible step (first EAS dev client build, Windows + EAS Mac cloud, no local Xcode) → Post-prebuild → Submit → Already done. Replaces the looser MVP.md sub-sequence as the active checklist; MVP.md retained as canonical pre-launch scope reference.

- **Apple Developer Individual → Org migration approved (5/21)**, Closes org-conversion thread opened 5/09, unblocked 5/11 (`thriftvaultapp.com` live with LLC WHOIS), submitted 5/16. Remaining: fill `ascAppId` + `appleTeamId` in `eas.json` with new Org team ID.
- **BUG FIX: Save-for-later round-trip clobbered scan date (5/21)** (`app/(tabs)/scan.tsx`), `openSavedItem` rebuilt session snapshot via `buildSessionSnapshot` minting fresh `Date.now()` `createdAt`; `createItemFromScan` set `item.date` to commit time. Fix: override restored snapshot's `createdAt` to `saved.savedAt`; derive `item.date` from tail of `itemSnapshots` (newest-first prepend so tail = original scan). Pattern lesson: `Date.now()` inside a "restore" path is almost always a bug; wrappers that rebuild session state must accept stored timestamps, not mint fresh.
- **Contact email migrated to brand domain (5/21)**, `thriftvaultapp@gmail.com` → `contact@thriftvaultapp.com` across 5 references (landing, in-app profile mailto, privacy + terms legal contact, ops table).
- **MCM-style refurb cap clamp + prompt rule expansion (5/21)** (`services/gemini.ts`), Unattributed MCM refurb scans (reupholstered Eames-style shell + polished chrome) stacking 30-50% premium past $300 ceiling into $400-$600. New `MCM_STYLE_RX` (MCM-style/Eames-style/Saarinen-style/Wegner-style/Danish modern/tulip/shell/womb/Barcelona/Jacobsen-style); cap $300 unrestored / $450 with `isCustomScan` refurb, gated on `!MCM_BRAND_RX` so authenticated maker-label scans pass through. Pattern lesson: when prompt expresses multiplicative premium, model treats band ceiling as soft anchor and stacks past it. Express premiums as HARD CEILING expansion ($300 → $450 max), not percentage multiplier; back with post-parse code clamp.
- **VISIBLE-ONLY NAMING HARD RULE + companion-item strip (5/21)** (`services/gemini.ts`), Eames 670 scans without 671 ottoman returning "Lounge Chair and Ottoman" (most iconic pairing biased the name). New VISIBLE-ONLY NAMING HARD RULE + FURNITURE PAIRED SETS block in COMMON HALLUCINATION TRAPS (670/671, chaise+ottoman, dining table+chairs, sofa+loveseat, bedroom set). Post-parse code strip regex-removes trailing companion-item phrases on `category === 'furniture'`. Pattern lesson: famous design pairings are the most predictable furniture-naming hallucinations; belt-and-suspenders: name trap in HALLUCINATION TRAPS + hard rule + post-parse strip.
- **Three-way red flag classifier + presentation helper (5/21)** (`services/gemini.ts`, `app/(tabs)/scan.tsx`, `app/detail.tsx`), 2-way `'verification' | 'counterfeit'` → 3-way `RedFlagKind = 'verification' | 'knockoff' | 'ai-listing'`. `RED_FLAG_KNOCKOFF_PREFIXES` enumerates physical-replica tells; `stock-photo` sentinel routes to `ai-listing`. New `getRedFlagPresentation()` returns `{kind, header, subtext, promptText, yes/noAccessibilityLabel}` so scan + detail share one source of truth. Pattern lesson: when a classifier branches UI copy, branches should be presentation-layer types returned from a single helper, not duplicated string literals across screens.
- **Red flag scope tightening: EXHAUSTIVE LIST + FURNITURE EXCLUSIONS (5/21)** (`services/gemini.ts`), Model coining ad-hoc flag categories outside the named 5 furniture + 5 homewares sets ("Visible surface wear, scratches at corners" appearing as redFlag instead of `sub` condition note). New EXHAUSTIVE LIST directive ("the five categories are the ONLY [category]-applicable; do NOT invent"). New 7-bullet FURNITURE EXCLUSIONS block (normal wear, finish loss, patina, drawer-front nicks, slight wobble, "looks old/vintage" without specific defect). Pattern lesson: open-ended list of flag examples invites extrapolation; bound the set with EXHAUSTIVE LIST + EXCLUSIONS naming common false positives by category.
- **`expo-image` migration across 5 image-heavy screens (5/21)** (`app/(tabs)/scan.tsx`, `app/(tabs)/index.tsx`, `app/detail.tsx`, `app/haul-detail.tsx`, `components/WebSidebar.tsx`), `react-native` `Image` + `ImageBackground` → `expo-image` `Image`. `resizeMode` → `contentFit`; no `ImageBackground` (wrap with View + absolute Image). Added `cachePolicy="memory-disk"` + `recyclingKey={String(item.id)}` on FlatList thumbs. Pattern lesson: `cachePolicy="memory-disk"` + `recyclingKey` are the load-bearing wins; default `memory` cache evicts on backgrounding, exactly when a thrifter switches back.
- **Hauls segment tap latency, ~1s → instant on repeat switches (5/21)** (`app/(tabs)/index.tsx`), Conditional ternary view switcher unmounted previous FlatList every tap (~48 cold `expo-image` decodes per mount). Fix: keep-mounted with `display: 'none'` toggle; `hasViewedHauls` gates first mount so off-screen view doesn't pay upfront cost. Tightened `initialNumToRender` 12→4, added `maxToRenderPerBatch=4` + `updateCellsBatchingPeriod=50`; `renderHaul` → `useCallback` so `HaulCard.memo` short-circuits. Pattern lesson: `A ? <X/> : <Y/>` is structural unmount cycle, not visibility toggle; "keep-mounted + display:none" gated by `hasViewed` gives instant subsequent switches.

- **Pottery + Antiques + Homewares expansion (5/20)**, New `'homewares'` ItemCategory + tiers 090-094 (Studio Pottery / Art Glass / Antique Metal Smalls / Antique Clocks / Pre-1900 Unmarked Ceramics) + 4 HARD RULEs (POTTERY HALLMARK & MAKER, GLASS SIGNATURE & ORIGIN, METAL HALLMARK, CLOCK MOVEMENT & ERA) + 3 code clamps (`isHandmadePottery`, `isCustomArtGlass`, `POTTERY_CONDITION_PENALTY_RX`) + 5 verification red flags (UNSIGNED GRAIL CLAIM, UNSIGNED ART-GLASS CLAIM, VISIBLE CONDITION DEGRADATION, HALLMARK MISSING, QUARTZ-IN-VINTAGE-CASE CLOCK). HOMEWARES EXCEPTION in HANDMADE_SUFFIX routes hand-built pottery to STUDIO POTTERY tier instead of generic labor formula. Drift-watch override: Etsy 2x for homewares (vs eBay 2x for apparel). Pattern: drift-watch platform-anchor mix splits per category, apparel's eBay-2x default isn't universal because Etsy is the curation-driven pool for ceramics/glass/antique smalls.
- **HANDMADE COORD SET EXCEPTION tier 097 (5/20)**, $40-$70 / $55-$95 / $80-$140 for two-piece handmade sets. `COORD_SET_RX` + `isHandmadeCoordSet` clamp gates `isHandmadeTop`/`isAlteredDress`/`isAlteredSkirt`/etc on `!isHandmadeCoordSet` so single-garment caps don't undercut the set tier. Multi-photo before/after classifier routes to COORD SET when after photo shows two coordinated garments from a single source. Pattern: gate per-garment clamps on set detection.
- **WORK-IN-PROGRESS DETECTION HARD RULE (5/20)**, 6 WIP tells (dress form context, sewing pins, safety-pin closure, raw threads, basting, workshop bg), requires TWO. Marks `sub` as "work-in-progress, not yet finished"; adds `RED_FLAG_VERIFICATION_PREFIXES` "Finish construction before listing..." (teal banner). Pattern: TikTok WIP-on-dress-form needs explicit detection or vision model defaults to describing partial garment as "like new".
- **Altered dress sub-tier refinement + fabric-aware cap tier 061 (5/20)**, single $40-$200 band → simple $30-$50 / moderate $50-$80 / complex $80-$140 (cap $200 → $140). `BASIC_DRESS_FABRIC_RX` caps at $80, `PREMIUM_DRESS_FABRIC_RX` unlocks $140, `COMPLEX_DRESS_CONSTRUCTION_RX` unlocks $140. **Superseded 5/24**: moderate further tightened to $40-$65, simple to $25-$40, basic-fabric cap dropped to $65 after user comp-check. Pattern: non-overlapping bands + worked example + fabric-aware code clamp = three-layer defense.
- **Design system polish: pressedOpacity token + `<BottomSheetModal>` extraction (5/20)**, `theme.pressedOpacity = { subtle: 0.6, default: 0.75, primary: 0.85 }`; 23 inline opacity literals swapped, reclassified by intent. New `<BottomSheetModal>` lifts PaywallModal pattern with internal `shouldRender` gate + `onAfterDismiss` callback. Migrated 4 modal callsites (haul-detail store + title, detail add-photo, index store-picker); 5 legitimately-fade modals untouched. Pattern: extracted modal wrappers need internal `shouldRender` so callers can flip `visible` directly without an onClose-callback dance.
- **Haul detail meta + long-press remove (5/20)**, `haulStoresLabel` inserted into headerMetaLine (`date · stores · N finds · $X spent`). `handleRemoveFromHaul` (defined at line 137 but unwired) wired to `onLongPress` on list + grid rows. Removes from haul without deleting from inventory (sets `date: ''`). Pattern: defined-but-unwired handlers are dead code, grep usage before assuming feature works.
- **Detail page Insights pills above description (5/20)**, Handmade + Before/After moved above `insightsSub` in expanded Insights card; mirror of 5/19 scan card pattern.
- **Cursor: duplicate-scan field-update in line with rescan (5/20)**, removed Math.max ratchet from "Update existing" flow, always updates name + resale (>0) + cat (guarded against 'other') per fresh AI verdict.

- **BUG FIX: Rescan paths dropped all photos beyond cover (5/19)**, `rescanAsHandmade` signature was `string` only; scan.tsx rescan handlers passed `stagedPhotos[0]`, detail.tsx handlers passed `snapshot.sourceImageUri` (singular). Multi-photo upcycle rescan regressed ~64% ($55→$20). Fix: signature → `string | string[]`, all 5 call sites pass full array; snapshots store both `sourceImageUri[0]` + full `sourceImageUris`. Pattern: features flowing through 5+ call sites should accept broadest input from day one; narrowing per-site silently degrades fidelity at sites that didn't get the full payload.
- **Reversed `ItemCategory = 'watches'` (5/19)**, Watches collapse back into 'accessories'. JEWELRY_RX already covers watch keywords. WATCH NAME CLAMP re-gated: `WATCH_NAME_KEYWORD_RX` + `parsed.category === 'accessories'` replaces watches gate. InventoryContext remaps legacy `rawCat === 'watches' → 'accessories'`. Pattern: if existing routing arm already detects sub-domain via keyword, dedicated category is redundant overhead.
- **Vault search structured date filter (5/19)**, `extractDateFilters()` consumes month-name+day (`April 3` / `Apr 3`) or numeric m/d (`4/3` / `4-3`) BEFORE tokenization, returns `DateFilter[]` + residual query. `dateStringMatchesFilters()` compares month+day (year ignored). Applied to both `itemMatchesQuery` and haul filter. Pattern: tokenization strips structure the matcher needs; date queries need pre-tokenization extraction to a structured filter so residual doesn't pollute other-field matching.
- **Active pills (Handmade, Before/After) above title in scan card (5/19)**, Pills render above `resultHeader` (only when isCustom or beforeAfterDetected) with `marginBottom: 10`; previously buried below long descriptions and cropped in TikTok screen recordings. Pattern: classification context (load-bearing for explaining price) belongs ABOVE the title in marketplace-style cards.
- **User-facing jargon swaps in `services/gemini.ts` (5/19)**, "silhouette" → "shape" in 4 emitted red-flag strings (jewelry DESIGNER KNOCKOFF, LUXURY BAG KNOCKOFF, DESIGNER BAG KNOCKOFF, Tiffany authFlag); internal detection criteria still say "silhouette" (model-facing precise). NWT → "new with tags" via `sub` schema prohibition enumerating NWT/NWOT/VGUC/EUC/BNIB. Pattern: user-facing output language and model-facing instruction language should diverge when domain jargon is precise for the model but opaque to non-experts.
- **Scan header "ThriftVault · Launching June" version tag (5/19)** (`app/(tabs)/scan.tsx:1968–1973`), Top-right caption typography + mauve color, for TikTok screen-recording recognition. Matches landing page hangtag copy.
- **Platform "Other" X clear button (5/19)** (`app/detail.tsx:1402–1423`), X icon absolute-positioned over TextInput (not wrapped in flex-row), shows when `item.platform.length > 0`. Pattern: absolute-position inline icons over styled TextInput rather than wrapping in a row, wrapper padding breaks visual height parity with sibling fields.
- **Pricing tier 089, Modern Mass-Market Small Furniture (5/18)**, Side tables/nightstands/bookshelves/TV stands/cabinets sold new at Walmart/Target/Wayfair budget $40-$120, priced $15-$50 used / $40-$80 refinished. ROUTING gate requires ≥1 positive vintage tell (hand-cut dovetails, mortise-and-tenon, real solid grain at unfinished interior, manufacturer plate from 60s-70s, antique-mall patina, weight commensurate with size) before escaping to vintage tier. New CONDITION REPORTING + FURNITURE COMPARTMENT NAMING HARD RULEs (name each compartment by type AND position; no lumping). Pattern: visual styling of "dark wood + small + traditional" is NOT a vintage tell; modern factories produce exactly this look at Walmart prices.
- **PHOTO CAPTURE ARTIFACTS + REAL TEXTILE/TRIM EXEMPTIONS on AI-GENERATED PHOTO (5/18)**, Decora/scene mirror-selfies, jirai full-body with plushie clutter, studio lingerie with Victorian-floral lace all false-flagging. Exemptions cover motion blur, bloom/halation, JPEG/TikTok compression banding, beauty-filter softness, J-fashion subculture styling (decora/scene/fairy kei/jirai kei/lolita/harajuku/kidcore) AND lace/embroidery/beading/jacquard/tulle/tie-dye. Both sit BEFORE AI CONTEXT RECOGNITION so they short-circuit signature matching. Filter-softened skin alone no longer trips; requires hair-edge fade AND one unrelated AI tell. Pattern: AI-detection carve-outs go BEFORE the signature/tell pipeline because once a signature matches the pairing threshold drops.
- **Pricing tier 088, Vintage Tableware/Dinnerware (5/18)**, Pyrex (Corning 1947-80s opal-glass), original Fiestaware (Homer Laughlin pre-1986), Wedgwood Jasperware, Spode, Noritake Art Deco "M-Mark", Roseville/McCoy/Hall art pottery, Royal Doulton HN, Scandinavian MCM, Depression/Carnival/Milk glass, Boleslawiec/Talavera/majolica. New TABLEWARE BACKSTAMP & ERA HARD RULE: maker's-mark ID required; post-1986 Fiestaware trap colors (periwinkle/sapphire/plum/raspberry/lemongrass) didn't exist 1936-1972; vintage Pyrex requires opal/milk-glass body (modern Instant Brands is clear borosilicate). Condition penalties: chip/crack -40-60%, crazing -20-35%, glaze loss -25-40%. Pattern: when a tier's most valuable sub-segment depends on a specific maker's mark, the auth rule must name era markers (mold marks, date codes, color palettes) that distinguish vintage from reissue.
- **`<InlinePromptButton>` component (5/18)**, Detail-screen Yes/No buttons stretched into oversized ovals after the 5/17 `minHeight: theme.minTouchTargetSize` + `borderRadius: full` + short content combo. Shared component canonicalizes paddingVertical:6 + paddingHorizontal:10 + radius.full + caption weight 600 + hitSlop:12 with variants accent/muted/danger/neutral. Replaced 10 Pressables across scan + detail; removed 8 orphan styles. Pattern: minHeight on a small button with full-radius produces an oval when content is short. Canonical mobile pattern is small visual + larger hitSlop, not larger visual.
- **Vault search overhaul (5/18)**, Single `itemMatchesToken / itemMatchesQuery` pair drives Flips/Closet + Hauls. Whitespace-split AND tokens, ~40-entry `SYNONYM_TO_CAT` (jeans→denim, purse→bags, kicks→shoes, coat→outerwear, earrings→accessories), normalized month-shortened item.date so "april" matches in Flips/Closet, quick-flag tokens (flagged/redflag/handmade/custom/upcycle/altered/reworked) on snapshot fields, `normalizeToken()` strips apostrophes/hyphens/periods/commas/slashes so "levis" matches "Levi's", price+category+status+platform exact match. Result count caption beneath search bar. Pattern: search-quality wins compound when they share one matcher; per-field if-chains drift and silently skip categories.
- **Pricing tier 086, Knit Bodycon/Off-Shoulder Mini Dresses (5/17)**, Unbranded ribbed-knit (off-shoulder/square-neck/scoop/cowl/halter, olive/sage/cream) was hitting Unknown $10-$30 floor; comps $18-$35. Unbranded $20-$32 / branded mid-tier (Princess Polly/PLT/Lulus/Boohoo/ASOS/F21) $25-$40. Q2 spring-knits spike pre-baked. Aritzia/Free People/Anthropologie/Reformation stay on contemporary tier. Pattern: trending-color + trending-silhouette unbranded combos need their own tier; the commodity-floor catch-all underprices anything currently moving on TikTok/IG.
- **Pricing tier 087, Modern Multi-Function Record-Player Cabinets + MODERN REPRO EXEMPTION (5/17)**, Crosley/Victrola/Innovative Tech/Wockoder/ION/Jensen repros ($80-$200 retail) priced $60-$120 via Generic vintage tier; comps $30-$75. Tells (any ONE): CD slot, cassette deck, AUX/USB/Bluetooth, plastic platter, mesh-grille speakers, decorative dial face, 14-24" footprint, MDF veneer. Authentic 60s-70s consoles ($150-$800+) separate via positive tells (4-6ft cabinet, real solid wood, die-cast platter, 100+lbs). PARTICLEBOARD MASQUERADE flag suppresses for matching units. Pattern 1: visual styling that references a vintage era is NOT vintage evidence; require positive vintage tells. Pattern 2: red flags should suppress when the price tier already accounts for the substrate concern, otherwise the warning becomes noise.
- **CAPTION/OVERLAY EXEMPTION on isCustom + beforeAfterDetected (5/17)**, TikTok narrative captions ("THEN I DID", "BEFORE/AFTER", "FIRST I... THEN I", "watch me transform", "from this to this", "POV: I made") tripped `beforeAfterDetected = true` → cascading `isCustom = true` → routing clean factory dresses through HANDMADE DRESS EXCEPTION ($80 vs real ~$25). Exemption inserted at TOP of isCustom rule; if all photos show matching construction and only "before" signal is text, classify as multi-angle. Screenshot memory broadened: screenshots never drive ANY classification. Pattern: every new classification signal must include caption-exemption carve-out at the TOP of the rule, not as a bottom-of-list disclaimer; bottom-of-list disclaimers get ignored when the bias direction at the top is strong.
- **`<Button>` component + Flips/Closet card unification + touch-target HIG pass (5/17)**, Variants primary/secondary/ghost × sizes md (48px)/lg (56px) + loading + icon left/right via theme tokens. Replaced 9 CTAs (markSoldBtn/errorBtn/addToClosetBtn/newHaulBtn/2×emptyBtn/emptyBtnSecondary/upgradeBtn/PaywallModal cta); removed ~80 lines orphan styles. Flips/Closet cards gained outer border (surfaceVariant 1px), `cardImageBlock` wrapper, `cardFooter` with lavender border-top, 2-line truncated name `flex: 1` left + right-aligned profit-colored price. Touch targets via `hitSlop {top:5, bottom:5}` (34→44px). Pattern 1: same-intent CTAs need component-level enforcement; per-screen styles drift even when devs reach for the same theme tokens. Pattern 2: shadow alone isn't enough to define card edge on cream bg; surface-on-cream dissolves without a border. Pattern 3: 2-line truncated name + right-aligned price reads as marketplace standard (Depop/Poshmark/Etsy).
- **MULTI-ZONE PRINTED TEXTILES carve-out on ALL-OVER DIGITAL PRINT (5/16)**, Y2K mesh maxi kaftan (animal print + tribal geometric + abstract painterly in adjacent panels on a single continuous fabric) was false-flagging. Existing exemptions covered single-pattern textiles + multi-fabric patchwork CONSTRUCTION, but not printed-patchwork (Sky to Moon / Charlotte Russe / F21 mall-tier 2005-2015 boho/festival kaftans, sarongs, coverups). Requires zones filled with textile repeats (no pictorial subjects), one continuous polyester/mesh/jersey, V-neck or deep-V maxi tells. Pictorial zones (characters, scenes, slogans) still flag. Pattern: textile-exemption lists need a "multi-pattern mosaic on single fabric" clause; treating each zone individually as exempt isn't enough because the model reads the combination as pictorial collage.
- **Pricing-drift watch, 85-tier registry with weighted rotation (5/16)**, 81+ tiers in `services/gemini.ts` previously spot-checked only at quarterly refreshes (next 2026-07-30); now HOT 12 every 2 weeks, WARM 24 every 8 weeks, COOL 49 every ~11 months, deterministic ISO-week hash drives slice selection (stateless). Registry table has per-row Depop/Poshmark/eBay queries (furniture rows substitute Chairish/1stDibs/Etsy). Routine prompt fetches both files via WebFetch each Monday, computes the week's 10-tier slice; code-level clamps (DIY Distressed Denim, BOOST STACKING, Confidence Downgrade, Crochet text-detection) get narrative sanity-check instead. Verdict: 15% out-of-band median OR 2+ platforms outside = drift; eBay weighted 2x; new band rounded per `roundDisplayPrice`. Pattern: routine reference data that changes frequently belongs in the repo (PR-editable, gitted, diffable); routine prompt that changes rarely stays in the schedule UI. Inverts the usual code-in-repo / prompt-in-config split because the agent is remote and its prompt is the harder thing to version.
- **PARTICLEBOARD MASQUERADE substrate-proof gate (5/15)**, `beforeAfterDetected = true` + any non-cover photo with raw wood grain (sanded surfaces, drawer interiors, end-grain, dovetail joinery) → SUPPRESS flag regardless of cover finish. Mirrors MCM bypass. Pattern: invert "don't speculate" exemptions into "use X as positive proof".
- **AI-GENERATED ARTWORK Tumblr false positive (5/15)**, Garbled-text tell gained UI/overlay exemption (mirror PHOTO branch); PRECONDITION requires positive ID of design; "checks" exemption broadened to neon/Y2K/Vans-style; platform list gained Tumblr (reblog/repost indicators). Pattern: sister branches with parallel tells need synchronized exemptions.
- **Y2K alt-fashion non-denim pants tier (5/15)**, Tripp NYC / Lip Service / Hot Topic Morbid / modern Y2K-revival flares with subculture detailing. Unbranded modern $25-$55, authenticated vintage $40-$120. Explicit "do NOT route through" Premium denim flare SPIKING / Authenticated Y2K premium denim / HANDMADE PANTS EXCEPTION. Pattern: pricing tiers need explicit do-not-route clauses per adjacent path.
- **isCustom carve-out for factory subculture detailing (5/15)**, "Waistbands added/removed" tightened to require visible modification evidence (raw cut edges, mismatched thread). Factory subculture detailing (checkerboard waistbands, D-rings, chain trim, ribbon-laced panels) on Y2K pants is factory-manufactured, NOT isCustom. Pattern: "when in doubt set isCustom=true" biases over-flagging, specific exclusions must name the bias.
- **Detail-page scan card asymmetry intentional (5/15)**, Scan card shows bold recommended + range; detail shows range on snapshot + auto-fills recommended into editable `item.resale`. Adding `suggestedResale` to ItemScanSnapshot would create three competing prices + stale frozen number once user edits.
- **Cloudflare Web Analytics chosen for landing page (5/15)** (planned, not wired). Free `<script>` beacon, no cookies/consent, no DNS migration. GoatCounter is the alternative. Privacy policy needs one-line disclosure once wired.
- **Removed `LEGACY_DEMO_ITEM_NAMES` migration filter (5/15)**, Dead code, pre-launch no users. `DEFAULT_ITEM_PLACEHOLDER_IMAGE` retained.
- **AI-GENERATED ARTWORK PRECONDITION (5/13)**, Branch firing on DIY-distressed jeans without print/graphic. PRECONDITION at top requires actual applied graphic design (paint/sublimation/screen-print/embroidery pictorial); exempts plain solids, distressing/rips/bleaching, classic textile repeats, construction details, brand labels. Dropped loose "photorealistic art style" tell.
- **Before/After denim back-view false positive (5/13)**, Added denim back-view to "Tells AGAINST (b)" + anatomical-concentration principle (front-only graphics, knee rips, back-yoke embroidery, single-sleeve paint are one-sided design, not a before).
- **Repurposed-textile outerwear sub-track (5/13)**, HANDMADE OUTERWEAR EXCEPTION split into FIBER-ART + REPURPOSED-TEXTILE/SEWN-FABRIC (blanket-jacket/quilt-coat/frankenjacket). (ii) minimum MODERATE $55-$120, complex $90-$180. Pattern lesson: handmade pricing exceptions need sub-tracks by construction type; fiber-art and sewn-fabric have different labor floors.
- **LOLITA/KAWAII J-FASHION carve-out on ALL-OVER DIGITAL PRINT (5/13)**, 4th "Do NOT flag" clause: lolita silhouette (puff/mutton/bishop sleeves + 2 of tiered ruffled skirt/multi-row lace/ribbon bows/pintucks/cinched waist/shirred back/matching headdress) + elaborate pictorial sublimation (carousels, sweets, castles, Alice, stained glass) is the defining genre aesthetic. Covers AP/BtSSB/AatP/IW/Meta/h.NAOTO + replica. Lolita is factory-made (isCustom=false). Out of scope: dedicated lolita pricing tier.
- **Legal docs at pretty URLs (5/14)**, `terms/index.html` + `privacy-policy/index.html`. Canonical thriftvaultapp.com/terms/ and /privacy-policy/. In-app refs updated across PaywallModal, profile, STORE_LISTING.md, MVP.md.
- **Kit waitlist form on landing hero (5/14)**, Direct AJAX POST to Kit `/subscriptions` (Form ID 9443345), no Kit JS/CSS. Custom markup (cream-warm input, brass-soft border, teal CTA). Success message mentions spam folder. Pattern lessons: skip provider's JS/CSS on static-site form integrations and POST directly; brand-new Kit accounts hit spam until sender reputation warms (~25-50 confirmed subs).
- **Hangtag design language extended page-wide (5/14)**, Punched-hole + twine top + brass-soft border + `4px 4px 14px 14px` radius + alternating 0.3-0.5° tilts that straighten on hover with translateY(-4px) lift. Applied to pricing cards (3-col), features (3x2), hero logo. Brass Save 33%/50% ribbons on Season Pass + Annual. Pattern: vintage motif can become page-wide visual language when consistently applied; dropping decoy "featured" emphasis (5/11) leaves cards equal, savings ribbons backfill hierarchy with truthful value signaling.
- **Trial strip color = teal-deep (5/14)**, Brass+cream fails WCAG AA (~2.5:1), brass+charcoal reads washed. Teal-deep ties to H1 italic + Notify Me button.
- **AI estimate disclosure surfaces (5/14)**, Onboarding slide 1 italic mauve note + Profile Settings "About AI estimates" row (sparkles icon, Alert.alert explanation). Scan card unchanged (existing range + Yes/No + low-data flag communicate uncertainty contextually). Pattern lesson: per-scan disclaimer dilutes existing uncertainty signals; one-time onboarding + permanent Profile section is honest hygiene without visible nag.
- **AI photo detection iteration deferred (5/13-5/14)**, 3 AI images (mirror-selfie composite, flat-lay sweatshirt, striped shirt) bypassed 4 prompt iterations. Edits still in place: paired tells, AI CONTEXT SIGNATURES A/B/C with lowered threshold, narrowed professional-photo exemption, bias line acknowledging 2026 AI prevalence. `__DEV__` instrumentation + `EXPO_PUBLIC_FORCE_CLAUDE_SCAN` parked 5/14, stripped 5/17.
- **DIY-distressed denim clamp (5/12)**, Split DENIM EXCEPTION simple band into SUBTRACTIVE ($20–$40, rips/holes/shredding) vs ADDITIVE ($25–$55, patches/dye/paint). NEW `isDIYDistressedDenim` clamp $45/$20. Pattern lesson: density of subtractive labor is NOT a craftsmanship signal, separate additive (skill) from subtractive (hours).
- **Pricing-tier drift watch routine launched (5/12)**, Weekly remote agent `trig_0113xK23HSeSpB46ySDYJtBF` (Mon 9am ET) fetches `PRICING_TIERS.md` and opens a drift-report PR. v1 had 14 fixed tiers; upgraded 5/16 to 85-tier weighted-rotation registry (see 5/16 entry for current state).
- **Domain `thriftvaultapp.com` live on GH Pages (5/11)**, Registered via Namecheap, WHOIS visibly tied to ThriftVault LLC (no privacy proxy). DNS: 4× A records + CNAME `www`. Legal docs auto-rehosted at `/assets/...`. Unblocked the Apple org-conversion reply.
- **Landing page polish (5/11)**, Pattern lesson: don't add CSS framing around an image that already has its own internal framing, they fight no matter which `object-fit` you pick. Pattern lesson: featured pricing-card emphasis should match best-value plan or be dropped entirely; middle-card weight creates unintentional decoy hierarchy. Pre-launch signaling at three depths (prelaunch-bar, status-badge, plans-note italic).
- **Apple org website + domain pick (5/09)**, Apple requires "valid organization website" for Individual → Org conversion. Picked `thriftvaultapp.com` over `.co` to block typo-traffic squatting. WHOIS must show ThriftVault LLC (no privacy proxy until Apple approves).
- **Landing page first build (5/09)**, Rebuilt via frontend-design skill into editorial vintage / curio-shop aesthetic. **Pattern: `thriftvault_logo.jpg` (v1) = wordmark for marketing surfaces; `thriftvault_logo_v2.png` (v2) = iOS app icon (camera-in-frame, no wordmark), don't confuse on web.**
- **Headline price lean-low (5/08)**, Replaced midpoint `(low+high)/2` with `low + (high-low)*0.3`. Resale comps cluster ~38% of range, not 50%.
- **Modern Levi's trucker tier + rescan default-lower bias (5/08)**, NEW Modern Levi's denim jacket tier ($30–$65 used, NWT $95) above Vintage Levi's line with "factory distressing ≠ vintage tell" callout. `RESCAN_CORRECTION_SUFFIX` default-lower bias: wrong-flags are usually overprice protests; generic "distressed/worn/strong brand" are NOT upper-tier signals.
- **Share card design staged (5/08)**, `SHARE_CARD_PLAN.md` 9:16 composed card (photo 65% + info 35% + watermark), 3 intent variants. `view-shot` + `expo-sharing` deferred to prebuild day (MVP step 10).
- **Handmade labor-hour formula → finished-look tier ladders (5/07)**, Removed `materials + hrs × $25 + 30% uniqueness + 20–30% trending` (overshoots unknown-maker Depop comps 30–80%). Added 6 finished-look exceptions: OUTERWEAR $35–$180, DRESS $30–$200, SKIRT $20–$140, BAG $20–$140, ACCESSORY $15–$120, FIBER-ART $25–$300. See `feedback_handmade_pricing.md`.
- **FIRST-PASS ANCHORING rule (5/07)**, Default position = band low + 30% width, NOT upper edge. Reserve upper third for explicit upper-tier signals (named maker, NWT, exceptional construction, mint). CONDITION DEFAULT: assume "used-good" unless damage or tags visible.
- **Custom outerwear / bags / caps clamps (5/07)**, `isCustomOuterwear` $180, `isCustomBag` $140, `isCustomCap` $80 (new `isCapText` regex). Covers altered factory bases + from-scratch handmade.
- **MCM bypass on particleboard clamp (5/07)**, `MCM_BRAND_RX` (eames/knoll/saarinen/cassina/vitra/nakashima/...) bypasses `PARTICLEBOARD_RX` for authenticated mid-century pieces with original laminate tops.
- **Signed-vintage costume jewelry tier (5/07)**, `SIGNED_COSTUME_RX` (Trifari/Coro/Weiss/Haskell/Eisenberg/Hobé/Whiting & Davis/...) caps $120 floor $20, before terminal no-hallmark branch.
- **Y2K viral brands exempt from boost-stacking (5/07)**, `LUXURY_EXEMPT_RX` extended to Juicy Couture/Von Dutch/Ed Hardy/Baby Phat/Apple Bottoms (legit Y2K tier $100–$200 was clamping to $130).
- **Furniture refurb red-flag false positives (5/07)**, Header rule: evaluate every flag against COVER PHOTO, not before/in-progress photos. PARTICLEBOARD: don't speculate under paint/refinish. HIDDEN ODOR exclusion expanded to painted/restained/limewashed/refinished wood. SCREENSHOT/UI EXEMPTION: glossy paint and "too clean" looks ≠ AI generation.
- **Save-for-later race, in-flight scan clobbers loaded saved item (5/07)**, `openSavedItem` aborts `abortControllerRef.current` and resets scanning flags before restoring; `if (controller.signal.aborted) return;` added after network awaits in `handleScanStaged` + `handleConfirmHandmade`. Pattern lesson: an `abort()` doesn't help if the fetch already settled, every async-`setResult` path needs both abort AND post-await `signal.aborted` guard.
- **Camo bolero overpricing, restructured-tops mis-routed (5/07)**, Multi-photo BEFORE/AFTER outcome now: `category = "tops" AND alteration STRUCTURAL → HANDMADE SEWN-FABRIC TOP EXCEPTION`. ALTERED FACTORY BASE reframed as "SURFACE DECORATION ONLY" with explicit ranges (light $25–$45 unbranded / $35–$60 branded; dense $40–$70 / $55–$90). Pattern lesson: any "$X / $Y" notation risks being read as a range, write explicit bands.
- **Haul item sort, newest first (5/07)**, `app/haul-detail.tsx` + `app/(tabs)/index.tsx` hauls memo: `sort((a, b) => b.id - a.id)`. Active-haul "I just scanned that" feedback beats stable visual fingerprint.
- **6 new pricing sub-tiers (5/06)**, Lululemon (Align $30–$100, Define $50–$185); Designer SLGs (LV wallets $80–$400, Hermès H belt $300–$1100, silk twill $120–$600); Vintage graphic/band tees $20–$220 (grail-tier $500–$2000+ flagged low-confidence, not auto-priced); Vintage sports jerseys (M&N $80–$300, Champion Reverse-Weave $40–$300); Doc Martens $40–$300; Sunglasses $25–$500+. Prompt-only.
- **Furniture false-positive red flags (5/06)**, HIDDEN ODOR restricted to upholstered fabric/leather/padded; hard surfaces (wood/wicker/rattan/metal/glass) exempt. STRUCTURAL DAMAGE requires unambiguous visible damage; patina alone doesn't trigger. "When in doubt, do NOT flag, false damage warnings erode trust."
- **Recent finds red flag badge (5/06)**, Mirror of Vault grid badge on `app/(tabs)/scan.tsx` Recent Adds; same `tv_prompt_dismissed_${id}.redFlagBanner` dismissal, 11px sized for 100×100 thumb.
- **Scan card corner clipping (5/06)**, `cameraBox` + `cameraBgImage`: removed redundant image borderRadius (24 vs container 22 exposed charcoal at corners); switched cameraBox bg charcoal → cream.
- **Delete scan with confirmation + snapshot fallback (5/06)**, `deleteActiveSessionSnapshot` mirrors detail.tsx pattern: filter active out of `sessionSnapshots`, switch to next, only `clearResultAndPhoto()` if last. `Alert.alert` confirm; `sessionSnapshotsRef`/`activeSessionSnapshotIdRef` synced for async-confirm-time reads.
- **Multi-photo classifier, multi-angle dolman false positive (5/06)**, `multiPhotoSuffix` "Tells AGAINST (b)" bullets: same restructured garment from different angle (asymmetric/dolman/halter look "plainer" from back), same scene/styling across photos (try-on session ≠ transformation), caption text describing technique ≠ visual evidence.
- **FINAL SANITY CHECK pre-return self-critique (5/06)**, `services/gemini.ts` end of pricing instructions: pull resaleHigh down if >30% over likely sold comp; tier-straddle ranges → pick tier and set confidence:low; cap handmade labor outputs against unknown-maker comps; re-check BOOST STACKING when 3+ boosts. Closing: sold comps are ground truth, formula is starting estimate.
- **Handmade sewn-fabric tops first-scan overpricing (5/06)**, Ported SEWN-FABRIC TOP EXCEPTION from `HANDMADE_SUFFIX` into main `PROMPT` with tightened bands (simple $20–$45, moderate $30–$60, complex $50–$95, ceiling $95). `isHandmadeTop` clamp cap $120 → $95, floor $25 → $20. Pattern lesson: pricing exceptions added in `HANDMADE_SUFFIX` need porting to main prompt, suffix only fires on user-confirmed handmade rescan.

- **Multi-photo before/after, shape-shift upcycles (5/05)**, `multiPhotoSuffix` loosened to "AT LEAST TWO of these are true" with shared fabric identity as primary tell. Covers tee → ruched/halter/corset, sweater → vest, jeans → skirt, curtains → garments, frankenpieces, furniture repurpose. "No shared fabric identity" added to Tells AGAINST.
- **`beforeAfterDetected` lost on rehydrate (5/05)**, `sanitizeSnapshot` (`InventoryContext.tsx`) wasn't copying field off `source`; same shape as 4/23 redFlags strip. Persists alongside `isCustom`/`authFlags`/`redFlags`.
- **Furniture before/after + refurb premium (5/05)**, Staged-photo classifier extended with furniture refurb tells (worn → limewashed, stained → boucle, recane, brass pulls). Outcome routing: `category = "furniture"` BEFORE/AFTER picks FURNITURE PRICING, not ALTERED FACTORY BASE. Refurb premium 20–50% → 30–50%.
- **ALL-OVER DIGITAL PRINT carve-outs (5/05)**, Hawaiian/watercolor/painterly/tropical florals + tie-dye exempted; NEW multi-panel override (patchwork/spliced/mismatched/fringe/lace-up/grommet beats bullet regardless of pictorial reading).
- **Jewelry scan accuracy (5/05)**, NEW `► JEWELRY PRICING` branch (signed costume $20–$120, gold-filled $20–$75, solid gold by karat, pearl tiers, watches split fashion/mid/luxury). NEW `JEWELRY HALLMARK, HARD RULE` (yellow ≠ gold, silver-tone ≠ sterling, sparkly ≠ diamond without 925/STER/14k/18k/PLAT). Clamps: no-hallmark $30, designer-without-stamp $150 (Tiffany/Cartier/VCA/Pandora/Yurman), watch-luxury-without-stamp $400 (Rolex/Omega/Patek/AP). NEW `HANDMADE JEWELRY EXCEPTION` $15–$180.
- **Bags + sneakers auth (5/05)**, NEW `BAG AUTHENTICATION, HARD RULE` per-brand (LV date code, Chanel 8-digit, Hermès blind, Goyard MAISON, Coach creed, Gucci/Prada/Dior plaques). NEW `SNEAKER AUTHENTICATION` splits BASE hyped (Jordan 1/Dunk Panda/Yeezy 350, silhouette OK) from COLLAB (Travis Scott/Off-White/Fragment/Sacai, require SKU). Clamps: luxury-bag-without-auth $300, sneaker-collab-without-auth $250.
- **`SECURITY_AUDIT.md` refresh (5/05)**, 5/03 Sentry DSN wire-up closed only open Low finding.
- **Red flag UI text false positives (5/04)**, Front-loaded `SCREENSHOT/UI EXEMPTION` gate; tightened text bullet to require text on PHYSICAL SURFACE + 2nd artifact. TikTok/Reels captions, hashtags, watermarks, foreign-script overlays were tripping garbled-text bullet.
- **Furniture scanning, full sweep (5/04)**, Added `'furniture'` as `ItemCategory` (subtype by keyword). NEW FURNITURE PRICING branch (brand/era tiers IKEA→MCM→antique, material signals, -30% size penalty, MCM ATTRIBUTION HARD RULE, designer name needs label/sticker/stamp, else "MCM-style" $80–$300). NEW furniture red flags (PARTICLEBOARD MASQUERADE, MCM KNOCKOFF, HIDDEN ODOR, BEDBUG, STRUCTURAL DAMAGE). Furniture upcycle carve-out (refinish/reupholster/recane allowed). Particleboard text → $80 clamp; furniture skips `confidenceFromRangeWidth`. `KNOWN_PLATFORMS` + CATEGORY_GROUPS extended.
- **Rescan `beforeAfterDetected` lost on single-photo rescans (5/03)**, Clamp required `images.length >= 2`; fix falls back to `priorResult?.beforeAfterDetected === true`. `rescanWrong` paths thread `priorResult`; `confirmHandmade` paths OR in prior verdict at call site. `services/gemini.ts:499`, `app/detail.tsx:444`, `app/(tabs)/scan.tsx:1538`.
- **Handmade sewn-fabric top tier (5/03)**, From-scratch sewn handmade tops returning $95–$180 vs real Depop comps $40–$80. NEW `HANDMADE SEWN-FABRIC TOP EXCEPTION` (simple $25–$55 / moderate $40–$85 / complex $60–$120). `isAlteredTop` → `isHandmadeTop`, material-tier-aware ($180 cap crochet/hand-knit/cottagecore, $120 cap default sewn). `isExceptionalDenim` override still wins.
- **"Knit" disambiguation (5/03)**, Bare `knit(ted)?` was routing factory knit fabric (jersey/spandex/ponte) to craft tier. Tightened to require `hand[-\s]?knit(ted)?` or `crochet|knitwear|yarn|cottagecore|milkmaid|mending|patchwork|embroidered|macrame|needlepoint`. Pattern: ambiguous textile terms need disambiguation in both prompt and code.
- **Sentry DSN wired (5/03)**, TestFlight cut from launch plan (insufficient tester pool). DSN pasted in `.env`, verified with temp `Sentry.captureException`. Pre-launch SECURITY_AUDIT.md / LAUNCH_OPS.md "before TestFlight" → "before App Store submit".
- **Scan background reverted to v1 logo (5/03)**, `SCAN_BG_SOURCE` (`app/(tabs)/scan.tsx:117`) back to `thriftvault_logo.jpg`; v2 PNG still in `app.json` paths and `WebSidebar.tsx`.
- **Boost-stacking guard for factory items (5/02)**, `BOOST_BUCKETS` (5 regexes) + `LUXURY_EXEMPT_RX` in `services/gemini.ts`: `!isCustomScan && !LUXURY_EXEMPT && boostCount >= 3` → per-category ceilings (denim $180, tops/dresses $130, bottoms $120, outerwear $200, shoes $200, bags $220, accessories $120). Worst-case before: vintage Y2K Diesel rhinestone flares → $172–$1,290 (7× over comp); after: $180. Prompt-side `BOOST STACKING, HARD RULE` as belt-and-suspenders. `HANDMADE_SUFFIX` untouched (labor formula dominant).
- **Confidence tied to range width (5/02)**, `confidenceFromRangeWidth(aiConf, low, high)` helper (`services/gemini.ts:33`), downgrade-only. >3.0× ratio caps `high → medium`; >4.0× caps to `low`. `low`/`medium` never upgrade. Prevents `$50–$1200 "high confidence"` lie when boost stacking blows out range.
- **Category update on rescanWrong (5/02)**, `app/detail.tsx:520` was writing `name`/`resale` but ignoring `result.category`. Added `catUpdate` guarded against `'other'` to prevent downgrading specific category to AI fallback. Handmade-confirm path intentionally untouched (signal is "wrong pricing tier", not "wrong item").
- **"Size guess" placeholder bug (5/02)**, Schema example `"Brief description (size guess, color, material, condition)"` (`services/gemini.ts:41`) made Gemini echo "Women's size guess" when size couldn't be estimated. Rewrote with explicit examples (`"Women's 8"`, `"Men's L"`, `"US 10"`) + omit-instead-of-echo instruction.
- **App logo swap to v2 + App Store compliance blocker (5/01)**, Repointed `app.json` icon/splash/Android adaptive/web favicon (4 paths) + `WebSidebar.tsx:83` from `thriftvault_logo.jpg` to `thriftvault_logo_v2.png`. Verified v2 specs: 834×836, Format32bppArgb (alpha), circular frame, fails App Store icon (needs 1024×1024 opaque RGB full-bleed; iOS applies its own mask). Splash + Android tolerate alpha but still need 1024×1024. Native icon updates won't render until next prebuild/EAS build.
- **Security audit + fixes (4/30)**, Public repo. Scrubbed personal email from `eas.json`, hardened `Sentry.init`, closed `.claude/settings.json` pnpm/yarn loophole. New `SECURITY_AUDIT.md` (10-step methodology).
- **Q2 2026 sold-price tier refresh (4/30)**, `services/gemini.ts:98–128` + baselines. Cross-platform Depop/eBay/Poshmark research (~1800 samples, 18 tiers, eBay 2× trust). NEW: Y2K viral, factory sneakers (Generic→Premium→Hyped→Designer-collab), vintage non-denim Americana, knit sets, boots, hats. Refined Carhartt rare-color, denim cut differentiation (flare SPIKING / skinny FALLING), vintage Levi's Big E selvedge $300–$590. Quarterly refresh routine fires 2026-07-30.
- **Background scan perceived-seamless retry (4/30)**, Catch suppresses toast (gated on `pendingRetryRef`); finally keeps spinner; AppState retry via `queueMicrotask` on resume.
- **Profit subtext hidden on grid cards (4/30)**, Commented out (not deleted) on Flips/Hauls. Profile stats strip retains totals.
- **Agent safety guardrails (4/29)**, Triggered by Cursor/Opus 4.6 prod-wipe incident. Three layers: `.claude/settings.json` hard-denies (EAS prod, prebuild, force-push, hard-reset, clean -f, branch -D, cloud/DB CLIs, destructive migrations, `node -e`/`--eval`/`-p`); `SAFETY.md`; CLAUDE.md pointer.
- **Ops docs (4/29)**, `LAUNCH_OPS.md`, `DEV_OPS.md` new, solo launch playbook + release engineering reference.
- **Haul titles (4/29)**, Optional per-haul title via side-car `tv_haul_titles: Record<string,string>`. 60-char cap; blank deletes. Cleanup hook in `removeItem`. Title-as-hero / date-as-subtext.
- **Red flag prompt false positives (4/28)**, Pink ruffled umbrella was triggering AI-PHOTO. Tightened "fabric texture that looks CG-rendered" to require 2nd artifact. Expanded "do NOT flag" guard to enumerate radial/concentric symmetry, ruffles, pleating, smocking, fan/petal/flower, parasol shapes.
- **Yes/No haptics (4/28)**, `Haptics.selectionAsync()` (soft tick) on every Yes/No tap.
- **Saved-for-later prompt dismissals (4/26)**, `SavedScanItem` gained `promptCustomDismissed?` + `promptWrongScanDismissed?`; `handleSaveForLater` snapshots, `openSavedItem` restores.
- **Dark mode warmth pass (4/26)**, `theme/colors.ts` shifted dark surfaces toward amber/Edison-bulb; primary text → warm ivory `#EDE7DF`. Brand teal held (any darker drops below WCAG AA on `onPrimary`). All ratios ≥14:1.
- **Detail "Add photos" pill (4/26)**, Pill below carousel (`surfaceVariant` bg, `vintageBlueDark` icon+text, height 34) beat overlay variants. Pattern: overlays fight photo content; chip pills below media don't.
- **Scan tap-to-fullscreen (4/26)**, Post-result, camera box opens swipeable fullscreen viewer. Disabled when `!result && stagedPhotos.length > 0`.
- **Hauls empty + Unlisted badge (4/26)**, Inline "New Haul" CTA on empty Hauls. Unlisted badge teal → `surfaceVariant` so Unlisted=grey, Listed=teal, Sold=green visually distinct.
- **Red flag false positives on upcycled garments (4/24)**, Added `UPCYCLE EXEMPTION`. Narrowed `ALL-OVER DIGITAL PRINT` (requires pictorial; excludes textile repeats). Narrowed `AI-GENERATED PHOTO` (added construction guard, social media UI overlays exempt).
- **Code cleanup pass (4/24)**, `tsc --noEmit --noUnusedLocals --noUnusedParameters` surfaced dead code. Removed unused imports across 7 files; deleted `constants/Colors.ts` + `components/Themed.tsx`. Held handlers still wired to future work. tsconfig flags NOT added.
- **PaywallModal 5 features (4/24)**, Expanded 4 → 5 bullets, added "Counterfeit & scam alerts on every scan", led first bullet with "Unlimited AI scans, pricing & unlimited vault" (anti-cap differentiator).
- **Red Flag UX + persistence bugs (4/23)**, AI-photo detection as third red-flag condition. Persistence bugs: `sanitizeSnapshot` was stripping `redFlags`; rescan paths in detail.tsx dropped `authFlags`+`redFlags`; rescan profit used template literals instead of `formatMoney()`. `clearResultAndPhoto` now aborts in-flight scans. Accessibility labels: 37 → 169 across 9 files.
- **Pre-launch compliance pass (4/21)**, `terms.html` + `privacy-policy.html` updated for LLC + TN law + Sentry disclosure. `app.json` `minimumOsVersion: "15.1"`. `eas.json` created. PaywallModal: Restore Purchases added (Apple requirement), `/3 mo` formatting, "Popular" badge dropped.
- **Sentry + ESM fix (4/19)**, `metro.config.js` created with `unstable_enablePackageExports: true` to fix Sentry's ESM resolution.
- **Android readiness (4/19)**, `ANDROID.md` created; assessed not ready (missing package, versionCode, PNG adaptive icon, EAS config, Play account, RevenueCat Google Play). iOS-first launch confirmed.
- **Background scan fix (4/17)**, Removed `abortControllerRef.current?.abort()` from AppState background handler (was killing successful scans mid-flight). `pendingRetryRef.current = false` set before `setResult(geminiResult)` in both success paths.
- **Handmade single-pass (4/17)**, Removed auto-rescan-as-handmade (two sequential Gemini calls, ~30s). Single-pass via prompt directive "Your price output is final." Scan time back to ~10s.
- **Photo dedup on rescan (4/12)**, Via `FileSystem.getInfoAsync` size comparison. Sync `soldStr`/`resaleStr` string state at every programmatic write site in `detail.tsx`.